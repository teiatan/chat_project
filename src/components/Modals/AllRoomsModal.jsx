import { ModalCover } from './ModalCover';

export const AllRoomsModal = ({ onClose }) => {
    return (
        <ModalCover onClose={onClose}>
            <div>AllRoomsModal</div>
        </ModalCover>
    );
};
